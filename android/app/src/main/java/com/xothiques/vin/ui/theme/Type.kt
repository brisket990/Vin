package com.xothiques.vin.ui.theme

import androidx.compose.material3.Typography

// Sticking to Material3's default type ramp (system font) keeps this app
// dependency-free for now; swap in a custom FontFamily here if desired later.
val VinTypography = Typography()
